{% extends 'base.html.twig' %}


{% block body %}
	<h1>Administratif</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ user.id }}</td>
			</tr>
			<tr>
				<th>Email</th>
				<td>{{ user.email }}</td>
			</tr>
			<tr>
				<th>Roles</th>
				<td>{{ user.roles ? user.roles|json_encode : '' }}</td>
			</tr>
			<tr>
				<th>Password</th>
				<td>{{ user.password }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-info m-2" href="{{ path('app_office_index') }}">Retour à la liste</a>

	<a class="btn btn-warning m-2" href="{{ path('app_office_edit', {'id': user.id}) }}">Modifier</a>

	{{ include('user/office/_delete_form.html.twig') }}
{% endblock %}
